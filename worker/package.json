{
  "name": "subtask-push",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute subtask-push-registry --remote --file=./schema.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  }
}
